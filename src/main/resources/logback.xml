<configuration>

	<property name="LOG_PATTERN"
		value="%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n" />

	<!-- ================= SERVICE ================= -->
	<appender name="SERVICE_FILE"
		class="ch.qos.logback.core.rolling.RollingFileAppender">
		<file>logs/service.log</file>
		<rollingPolicy
			class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
			<fileNamePattern>
				logs/service_%d{yyyyMMdd}.log
			</fileNamePattern>
		</rollingPolicy>
		<encoder>
			<pattern>${LOG_PATTERN}</pattern>
		</encoder>
	</appender>

	<appender name="CONTROLLER_FILE"
		class="ch.qos.logback.core.rolling.RollingFileAppender">
		<file>logs/controller.log</file>
		<rollingPolicy
			class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
			<fileNamePattern>
				logs/controller_%d{yyyyMMdd}.log
			</fileNamePattern>
		</rollingPolicy>
		<encoder>
			<pattern>${LOG_PATTERN}</pattern>
		</encoder>
	</appender>

	<!-- ================= REPOSITORY ================= -->
	<appender name="REPO_FILE"
		class="ch.qos.logback.core.rolling.RollingFileAppender">
		<file>logs/repository.log</file>
		<rollingPolicy
			class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
			<fileNamePattern>
				logs/repository_%d{yyyyMMdd}.log
			</fileNamePattern>
		</rollingPolicy>
		<encoder>
			<pattern>${LOG_PATTERN}</pattern>
		</encoder>
	</appender>

	<!-- ================= PACKAGE BINDING ================= -->
	<logger name="com.yourapp.service" level="INFO"
		additivity="false">
		<appender-ref ref="SERVICE_FILE" />
	</logger>

	<logger name="com.yourapp.controller" level="INFO"
		additivity="false">
		<appender-ref ref="CONTROLLER_FILE" />
	</logger>

	<logger name="com.yourapp.repository" level="INFO"
		additivity="false">
		<appender-ref ref="REPO_FILE" />
	</logger>

	<root level="WARN" />
</configuration>
